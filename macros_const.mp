def rouge     = red enddef;
def vert      = green enddef;
def bleu      = blue enddef;
def jaune     = (1,1,0) enddef;
def violet    = (1,0,1) enddef;
def cyan      = (0,1,1) enddef;
def noir      = (0,0,0) enddef;
def orange    = rouge+jaune enddef;
